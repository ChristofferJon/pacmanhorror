#ifndef __GFS__
#define __GFS__

#include <string>
#include <vector>

using std::string;
using std::vector;

#include "GameEntity.h"

class GFS
{
public:
	GFS(string _file);
	~GFS();

private:

};

#endif